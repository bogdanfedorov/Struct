{
  "name": "struct",
  "version": "0.1.0",
  "description": "Immutable, hidden-class-stable structural records for V8 — fork/branch/lineage without prototype-chain hidden-class fragmentation.",
  "license": "MIT",
  "author": "Bogdan Fedorov",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --allow-natives-syntax --test spec/*.spec.js",
    "example": "ts-node example/struct.example.ts",
    "bench": "npm run build && node bench/run-tiers.js",
    "bench:no-opt": "npm run build && node --no-turbofan --no-maglev bench/tiers.bench.js no-opt",
    "bench:maglev": "npm run build && node --no-turbofan --maglev bench/tiers.bench.js maglev-only",
    "bench:turbofan": "npm run build && node --turbofan --no-maglev bench/tiers.bench.js turbofan-only",
    "bench:turbofan-maglev": "npm run build && node --turbofan --maglev bench/tiers.bench.js turbofan+maglev",
    "bench:memory": "npm run build && node --expose-gc bench/memory.bench.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
