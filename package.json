{
  "name": "struct",
  "version": "0.1.0",
  "description": "Immutable, hidden-class-stable structural records for V8 — fork/branch/lineage without prototype-chain hidden-class fragmentation.",
  "license": "MIT",
  "author": "Bogdan Fedorov",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --allow-natives-syntax --test spec/*.spec.js",
    "example": "ts-node src/example/struct.example.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
